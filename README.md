# Gold Trading Auto FTE (OpenAI Only)

## Setup

1. Copy .env.example to .env
2. Add your OPENAI_API_KEY
3. Install requirements:
   pip install -r requirements.txt
4. Run:
   python app.py

For dashboard:
   streamlit run dashboard/dashboard_app.py
